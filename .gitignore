    # Node.js
    node_modules/
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    package-lock.json # Optional: Some commit this, some don't. If unsure, ignore it.
    build/
    dist/

    # Python
    venv/
    *.pyc
    __pycache__/
    *.env # Ignore environment variables files
    .env.*
    *.log

    # OS generated files
    .DS_Store
    Thumbs.db

    # Temporary files (like from our Python service)
    # Not strictly needed if cleanup works, but good practice
    *.tmp 